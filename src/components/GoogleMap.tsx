/**
 * TripNoute v2 - GoogleMap Component
 * 
 * Re-exports MapboxMap for backward compatibility.
 * All map functionality now uses Mapbox GL JS.
 */

'use client';

import MapboxMap from './MapboxMap';

export default MapboxMap;
